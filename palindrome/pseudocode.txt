given string, check is palindrome

 > brute 
    let new_reverse_str = in built reverse method
    return string == new_reverse_str


 > optimized
    left = 0
    right = string.length - 1
    while loop until left < right
        if string(left) == string(right) 
            left ++
            right --
        else 
            return false
    return true
given sorted arr, remove duplicate

 > brute
    let new_arr = [arr[0]]
    for loop i = 1 until i <= arr.length - 1
       if new_arr[new_arr.length - 1] != arr[i]
            new_arr.push(arr[i])
    

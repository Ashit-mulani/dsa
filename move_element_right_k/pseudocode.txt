 given arr and k, move elements right by k step

 > brute
    for loop i = 0 until i < k
        copy = arr[arr.lenght - 1]
        for loop i = arr.length - 1 until i > 0
            arr[i] = arr[i - 1]
        arr[0] = copy
